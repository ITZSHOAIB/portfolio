import type { WritingPost } from "$lib/types/portfolio";

// Keep the source empty until real Markdown/MDX posts are added.
// Future implementation can replace this with import.meta.glob over src/content/writing.
export const writingPosts = [] satisfies WritingPost[];
